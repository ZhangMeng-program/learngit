Git is a distributed version control system.
git is free softwar distributed under the GPL.
Git has a mutable index called stage.

Now I add an new line to test the diff command

And then this is not use the git add command to test the git diff

Git tracks changes
